schema_version = 1
id = "humanoid-m38-zero-command-source"
allowed_paths = ["tests", "scripts"]
artifacts = []
evidence = [{ label = "unittest-ok", source = "stdout", contains = "OK" }]
[execution]
argv = ["python3", "-m", "unittest", "-v", "tests.test_m38_adapter_zero_command_source"]
cwd = "."
timeout_seconds = 60
output_max_bytes = 16384
[exit]
codes = [0]
